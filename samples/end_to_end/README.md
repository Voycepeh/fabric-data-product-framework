# Minimal end-to-end sample assets

Use these files with the template notebooks in sample mode (`USE_SAMPLE_DATA = True`):

- `minimal_source.csv`
- `minimal_source_contract.py` (`MINIMAL_SOURCE_CONTRACT` fixture)
