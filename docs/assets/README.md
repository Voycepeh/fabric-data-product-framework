# README image assets

Upload generated README images into this folder using these exact filenames:

| Filename | Used for |
|---|---|
| `framework-benefits.png` | User benefits infographic. |
| `before-after.png` | Before and after adoption infographic. |
| `mvp-flow.png` | End-to-end MVP flow infographic. |

The root `README.md` already references these files, so the images will render automatically after upload.
