# Runtime API Reference

`fabric_data_product_framework.runtime` builds run identifiers, validates notebook naming policy, and constructs runtime context payloads used across stages.

::: fabric_data_product_framework.runtime
    options:
      filters: ["!^_"]
